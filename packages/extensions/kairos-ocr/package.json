{
  "name": "@kairos/extension-ocr",
  "version": "0.1.0",
  "private": true,
  "description": "Kairós OCR — extrai texto de imagens via OpenRouter vision (zero setup, usa a chave OpenRouter existente). Suporta PNG, JPG, WEBP, GIF.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@kairos/agent": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}